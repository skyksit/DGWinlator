# Local test game payload. Commercial game data - must never reach this public
# repository. Kept here only because dgplayer.ini authoring is done against it.
/sizuku.zip

# Android Studio project state (the root project is opened as its own IDE project).
/.idea/
